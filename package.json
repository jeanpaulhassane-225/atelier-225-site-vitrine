{
  "name": "atelier-225-site-vitrine",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.20.8"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "verifier": "npm run check && npm run build",
    "deploy": "npm run build && npx wrangler@4.129.1 pages deploy dist --project-name=atelier-225-site-vitrine",
    "postinstall": "node scripts/setup-hooks.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "@fontsource-variable/archivo": "^5.3.0",
    "@fontsource-variable/inter": "^5.0.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.6.0"
  }
}
